[defaults]
inventory = inventory
roles_path = ../roles
host_key_checking = False
retry_files_enabled = False
interpreter_python = auto_silent

# Do not enable become globally — playbooks enable it where root is required.

[privilege_escalation]
become_exe = /usr/bin/sudo.ws
