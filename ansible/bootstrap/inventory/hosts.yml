---
# Host connection details (IP, user) live in inventory/host_vars/<name>/local.yml
# "chris" here is only Ansible's internal name — SSH always uses ansible_host (IP).
all:
  children:
    home_servers:
      hosts:
        chris:
