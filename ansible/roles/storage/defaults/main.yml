---
# External disks are optional. Each entry is independent, so services can use different
# disks or continue using the system disk when this list is empty.
storage_disks: []

# Example:
# storage_disks:
#   - name: media
#     source: /dev/disk/by-uuid/<uuid>
#     mount_point: /mnt/media
#     fstype: ext4
#     mount_opts: defaults
#     format: false
